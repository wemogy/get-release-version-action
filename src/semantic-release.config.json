{
    "semantic_release": {
        "tag_format": "{{PREFIX}}{version}",
        "branches": {
            "all": {
                "match": ".*",
                "prerelease": false
            }
        }
    }
}
